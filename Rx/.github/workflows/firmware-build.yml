name: firmware-build
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

permissions:
  contents: read

jobs:
  arduino-cli:
    name: arduino-cli compile (ESP32-C6)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install arduino-cli
        run: |
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh \
            | sh -s -- -b /usr/local/bin

      - name: Install ESP32 board package
        run: |
          arduino-cli core update-index --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://espressif.github.io/arduino-esp32/package_esp32_index.json

      - name: Install libraries
        run: |
          arduino-cli lib install "RadioLib"
          arduino-cli lib install "ArduinoJson"
          arduino-cli lib install "Crypto"

      - name: Stage shared headers
        run: |
          if [ -d "../shared" ]; then
            cp ../shared/*.h SoldierNode/
          else
            echo "::warning::shared/ not found at ../shared — sketch may fail to compile."
          fi

      - name: Compile SoldierNode
        run: arduino-cli compile --fqbn esp32:esp32:esp32c6 --warnings all SoldierNode